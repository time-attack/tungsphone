# Phase 1 (control proof) only needs: openai, httpx, python-dotenv
openai>=1.40
httpx>=0.27
python-dotenv>=1.0

# Moss semantic UI grounding (host sponsor). Imports as `moss`.
moss>=1.4.0

# Phase 2 (LiveKit voice agent)
livekit-agents>=1.0
livekit-plugins-minimax
livekit-plugins-deepgram
livekit-plugins-openai
livekit-plugins-silero

# token_server.py (only if you self-issue LiveKit tokens for SahurKit)
livekit-api>=0.8
flask>=3.0

# macOS port (sahur_mac.py): the floating buddy (PyObjC). Control itself is just
# AppleScript via osascript — no extra deps, no Swift, no server. (Harmless on
# non-Mac; simply unused.)
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=10.0; sys_platform == "darwin"
